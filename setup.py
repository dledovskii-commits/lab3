from setuptools import setup, find_packages

setup(
    name="fibonacci-lab3",
    version="1.0.0",
    description="Fibonacci sequence generator for lab work",
    packages=find_packages(where="."),
    python_requires=">=3.6",
)